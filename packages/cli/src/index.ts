// Programmatic exports for downstream consumers (Platform preview, tests).
// Phase 0: empty surface — the CLI is consumed via the `nebula-docs` binary.
// Phase 3+ may expose the Astro integration so a tenant can add their own
// `nebula.config.ts` extensions.
export {};
