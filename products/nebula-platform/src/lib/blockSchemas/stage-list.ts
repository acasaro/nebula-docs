import { ListChecks } from 'lucide-react';
import type { BlockAttrSchema } from './types';

export const stageListSchema: BlockAttrSchema = {
  blockType: 'mdxStageList',
  title: 'Edit Stage List',
  headerIcon: ListChecks,
  sections: [],
};
