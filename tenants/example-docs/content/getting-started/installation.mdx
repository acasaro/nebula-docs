---
title: 'Installation'
description: 'How to install and run Example Docs locally.'
icon: 'download'
---

# Installation

<Snippet file="disclaimer" />

## Prerequisites

- Node.js 22 or later
- pnpm 10 or later
- A Nebula Docs CLI install (`@nebula-docs/cli`)

## Steps

<Steps>
  <Step title="Clone the repository">
    Pull the tenant repo from GitHub.

    ```bash
    git clone git@github.com:your-org/example-docs.git
    cd example-docs
    ```
  </Step>
  <Step title="Install dependencies">
    Run pnpm install in the project root.

    ```bash
    pnpm install
    ```

    <Callout type="info">
      The install pulls `@nebula-docs/cli` and any tenant-specific React components in `components/`.
    </Callout>
  </Step>
  <Step title="Start the dev server">
    The CLI's dev server gives you HMR-backed local rendering.

    ```bash
    pnpm nebula-docs dev
    ```

    Open [http://localhost:4321](http://localhost:4321) in your browser.
  </Step>
  <Step title="Build for production">
    When you're ready to ship, run the build.

    ```bash
    pnpm nebula-docs build
    ```

    The static output lands in `dist/`. Your CI workflow uploads that directory to your OOSS bucket.
  </Step>
</Steps>

## Next steps

- Review the [Components](/components/cards) section for examples of every block.
- See the [API Reference](/api/users) for how to document an endpoint.

<Callout type="warning">
  This tenant is a synthetic fixture. Don't use this content for real documentation.
</Callout>
