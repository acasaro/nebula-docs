---
title: 'Users API'
description: 'User-related endpoints for the synthetic Example service.'
icon: 'users'
---

Endpoints for managing users in the Example service.

## GET /users/&#123;id&#125;

Retrieve a single user by ID.

### Path parameters

<ParamField path="id" type="string" required>
  The user's unique identifier (UUID v4).
</ParamField>

### Query parameters

<ParamField query="include" type="string">
  Comma-separated list of related resources to include. Supported values: `posts`, `roles`, `organization`.
</ParamField>

### Response

<ResponseField name="id" type="string">
  The user's unique identifier.
</ResponseField>

<ResponseField name="email" type="string">
  The user's email address.
</ResponseField>

<ResponseField name="name" type="string">
  The user's display name.
</ResponseField>

<ResponseField name="roles" type="string[]">
  Array of role identifiers granted to the user.
</ResponseField>

<ResponseField name="createdAt" type="string">
  ISO 8601 timestamp of when the user was created.
</ResponseField>

### Example

<RequestExample>
  ```bash
  curl -X GET https://api.example.com/users/123 \
    -H "Authorization: Bearer YOUR_TOKEN"
  ```
</RequestExample>

<ResponseExample>
  ```json
  {
    "id": "123",
    "email": "user@example.com",
    "name": "Jane Doe",
    "roles": ["editor", "reviewer"],
    "createdAt": "2026-01-15T08:30:00Z"
  }
  ```
</ResponseExample>

<Callout type="warning">
  This is synthetic API documentation. The endpoint does not exist.
</Callout>
