---
title: Tooltip
description: Inline trigger with a hover popup — optional title, description, and CTA link.
---
`<Tooltip>` decorates a piece of inline text with a popup that appears on hover or focus. Use it for short hints, definitions, or a "learn more" affordance that doesn't earn its own line.

## Hint only

The <Tooltip title="Application Programming Interface">API</Tooltip> reference describes every endpoint.

## With description

Hover the underlined term: <Tooltip title="HTTP" description="The protocol the web is built on. Stateless, request/response.">HTTP</Tooltip> requests are independent.

## With a CTA link

Click through for more depth: <Tooltip title="Webhooks" description="Server-to-server callbacks fired on events." cta="Read the guide" href="/api">webhooks</Tooltip> are how you react to events asynchronously.

## Positioning

Default is `side="top"` `align="center"`. Override either:

- <Tooltip title="Bottom-aligned" side="bottom">below</Tooltip>
- <Tooltip title="Right side, start-aligned" side="right" align="start">right-start</Tooltip>
- <Tooltip title="Left side" side="left">left</Tooltip>
